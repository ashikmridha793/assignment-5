1️⃣ What is the difference between var, let, and const?
    Ans: (var) is function-second and can be redeclared, while (let) and (const) are  block-scoped; (let) allows re assignment of valueas (const) is for variables that should remain constant.

2️⃣ What is the spread operator (...)?
    Ans: The spread operator allows an iterable, such as an array or object, to be expanded into indevidual elements or properties for easy copying and merging.

3️⃣ What is the difference between map(), filter(), and forEach()?
    Ans: (forEach()) simply execute a function for each element without returning anything , (map()) transforms each element and returns a new array, and (filter()) returns a new array countining only the elements that meet aspecific condition.

4️⃣ What is an arrow function?
    Ans: An arrow function provides a more concisie syntax for writing function expression and does not have its own this context, making it edeal for short, one-line operations.

5️⃣ What are template literals?
    Ans: Template literals are strings enclosed in backticks (`) that allow for envedded expressions using ${} and easy creation of multi-line string without spacial escape characters.